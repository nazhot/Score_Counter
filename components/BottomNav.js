import { React } from 'react';
import { StyleSheet, Text, View, SafeAreaView } from 'react-native';

const styles = StyleSheet.create({
    container: {
        backgroundColor: "#a02",
        flex: 1,
    }
});

const BottomNav = ( {screens} ) => {
    return (
        <View
        style={styles.container}
        >

        </View>
    );
};

export default BottomNav;